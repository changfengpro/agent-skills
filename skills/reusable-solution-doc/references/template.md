# 通用解决方案文档模板

复制下方骨架填充。`<...>` 为占位符,需替换为类别描述或通用命名,**不要填入具体工程/文件/库名**。删去不适用的小节。

---

# <主题>:<一句话场景定位>

适用场景:<问题发生的通用条件,例如"基于 X 构建的工程,头文件位于非标准目录或为构建期生成">。
工具 / 环境:<相关工具及版本约束>。
日期:<YYYY-MM-DD>。

---

## 1. 问题现象

<读者用来对号入座的可观察症状>。常见于以下情形:

- <典型触发情形 A>
- <典型触发情形 B>

## 2. 根因

<一句话定性根本原因>。多个并列原因时用对照表:

| 类型 | <失败原因 / 机制> |
|---|---|
| <类别 A> | <为什么会失败> |
| <类别 B> | <为什么会失败> |

结论:<要做什么才能从机制上解决>。

## 3. 解决步骤

### 3.1 <步骤标题>

```bash
<命令,名字用占位符>
```

### 3.2 <步骤标题>(关键)

<为什么这一步是前置条件 / 易漏点>。

```json
<配置片段,路径用 ${变量} 或 <占位符>>
```

## 4. 验证清单

1. <可勾选判据 1>
2. <可勾选判据 2>

## 5. 维护要点

- <什么后续操作会让它再次失效,需重做哪步>
- <产物的持久性 / 版本库纳管情况>

## 6. 备注

<相关但非主线的坑 / 无害噪声>:<现象> —— <可忽略,或如何消除>。

## 7. 备选方案(不推荐)

<次优做法>:

```text
<片段>
```

缺点:<明确写出不推荐的理由>。优先用上文主方案。

## 8. (可选)其他工具 / 引擎的等价做法

<同一问题在替代工具下的等价配置>。
